export default function Header() {
    return (
        <header className="w-full h-[80px] fixed top-0 left-0 bg-gradient-to-r from-gray-900 to-black shadow-lg flex items-center justify-center z-50">
            <h1 className="text-4xl font-bold text-white tracking-wide">Sorting Visualizer</h1>
        </header>
    );
}